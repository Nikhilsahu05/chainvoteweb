import 'package:flutter/material.dart';

class CandidateResultScreen extends StatelessWidget {
  const CandidateResultScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
