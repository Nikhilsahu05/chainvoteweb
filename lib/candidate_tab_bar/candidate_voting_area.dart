import 'package:flutter/material.dart';

class CandidateVotingAreaScreen extends StatelessWidget {
  const CandidateVotingAreaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
